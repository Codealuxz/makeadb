services:
  makeadb:
    build: .
    ports:
      - "3737:3000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - makeadb-data:/app/data
    environment:
      - PORT=3000
      - DB_HOST=${DB_HOST:-localhost}
    restart: unless-stopped

volumes:
  makeadb-data:
